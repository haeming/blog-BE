spring.application.name=blog-backend
spring.config.import=optional:application-secret.properties

# PostgreSQL Database 연결 설정
spring.datasource.url=jdbc:postgresql://home.haemstory.com:5432/blog
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA 설정
# update 된 설정 나중에 배포할 때 버전 다르게 만들어보기
spring.jpa.hibernate.ddl-auto=update 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# 웹
server.port=8080

# 모바일
server.address=0.0.0.0

# 파일 업로드 설정
file.upload-dir=/uploadFiles/
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=500MB

# JWT 설정
jwt.secret=dkanrjskdjWlehldjTrjsrlfrpTjqhausehlsmszldlqslek
jwt.expiration=180000000

logging.level.org.springframework.security=DEBUG